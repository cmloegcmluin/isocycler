import {Edo, Max, Norm, Periods, Rpd} from "../puns/types"

const DEFAULT_EDO = 12 as Edo
const DEFAULT_MAX_NORM = 5 as Max<Norm>
const DEFAULT_MAX_RPD = 0.001 as Max<Rpd>
const DEFAULT_PERIODS = 2 as Periods
const DEFAULT_ET = true

export {
    DEFAULT_EDO,
    DEFAULT_MAX_NORM,
    DEFAULT_MAX_RPD,
    DEFAULT_PERIODS,
    DEFAULT_ET,
}
